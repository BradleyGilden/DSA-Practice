#include "monty.h"

void rotl(stack_t **head, unsigned int line_number)
{
    stack_t *ptr = NULL, *end = NULL;
    if (head == NULL || *head == NULL || (*head)->next == NULL)
        return;

    ptr = *head;
}
